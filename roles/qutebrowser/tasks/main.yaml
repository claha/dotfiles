---
- name: Install package
  become: true
  ansible.builtin.package:
    name: "{{ item }}"
    state: latest
  loop: "{{ qutebrowser_packages }}"

- name: Link config files
  ansible.builtin.include_role:
    name: link_config
  vars:
    config_files: "{{ qutebrowser_config_files }}"
