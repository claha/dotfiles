---
- name: Install package
  become: true
  ansible.builtin.package:
    name: "{{ item }}"
    state: latest
  loop: "{{ i3_packages }}"

- name: Install aur package
  ansible.builtin.include_role:
    name: aur_package
  vars:
    aur_packages: "{{ i3_packages_aur }}"

- name: Link config files
  ansible.builtin.include_role:
    name: link_config
  vars:
    config_files: "{{ i3_config_files}}"
